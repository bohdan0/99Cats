<table>
  <th>
    <%= @cat.name %>
  </th>
  <tr>
    <td>Birth date</td>
    <td>Color</td>
    <td>Sex</td>
  </tr>
  <tr>
    <td><%= @cat.birth_date %></td>
    <td><%= @cat.color %></td>
    <td><%= @cat.sex %></td>
  </tr>
</table>
  <p>Description:</p>
  <text>
    <%= @cat.description %>
  </text>

<table>
  <tr>
    <td><%= link_to 'Back to cats', cats_url %></td>
    <td><%= link_to 'Edit this cat', edit_cat_url(@cat) %></td>
  </tr>
</table>
